import React from 'react'
import {Link} from "react-router-dom";
import {transformCategory} from 'utils'

const SubAccount = ({subAccount}) => {
    return (
            <div className="a1c1_item">
                <Link to={`/autostore/${subAccount.id}`}>
                <div className="a1c1_item_img">
                    <img src={`http://localhost:3999/subaccount-photo/getSubAccountPhotos?subAccountId=${subAccount.id}`} alt={"Нет фотографии"}/>
                    {
                        //<div className="love"/>
                    }
                </div>

                </Link>
                <div className="a1c1_item_tet">

                    <p className="a1c1_item_tet1">{transformCategory(subAccount.category.title)}</p>
                    <h3>{subAccount.title}</h3>
                    <a href={`tel:${subAccount.contacts.phoneNumber}`}>{subAccount.contacts.phoneNumber}</a>
                    <p className="a1c1_item_tet2">ул. {subAccount.contacts.addresses[0].street}, д. {subAccount.contacts.addresses[0].houseNumber}</p>
                </div>
            </div>
    )
}

export default SubAccount
