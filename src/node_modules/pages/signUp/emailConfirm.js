import React, {useContext} from "react";
import {CurrentUserContext} from "contexts/currentUser";

const EmailConfirm = () => {

    const [currentUserState] = useContext(CurrentUserContext)

    return (
        <div className="modal modal_email" style={{display: 'flex'}}>
            <div className="modal_bg"/>
            <div className="modal_box">
                <img className="close mod_close" src="/img/close.svg"  alt={"close"}/>
                    <h2>Подтвердите Ваш E-mail</h2>
                    <p>На Вашу электронную почту <span className="red">{currentUserState.email}</span> отправлено письмо, для
                        подтверждения необходимо перейти по указанной ссылке</p>
                    <a href={"https://yandex.ru/"} className="hover2">Проверить почту</a>
            </div>
        </div>
    )
}

export default EmailConfirm
