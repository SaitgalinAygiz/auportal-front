import React from 'react'
import SubAccount from 'pages/globalFeed/components/subAccount'

const Feed = ({subAccounts}) => {
    return (
        <div className="a1c1_row active">
            {subAccounts.map((subAccount, index) => (
                <SubAccount subAccount={subAccount} key={index} />
            ))}
        </div>
    )
}

export default Feed
